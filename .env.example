
Now, here's the `.env.example` file:

```text name=.env.example
# GitHub Authentication
# Create a token at: https://github.com/settings/tokens
# Required permissions: repo, read:org
GITHUB_TOKEN=your_github_personal_access_token_here

# Organizations to analyze (comma-separated)
GITHUB_ORGANIZATIONS=AOSSIE-Org,StabilityNexus

# Optional: GitHub usernames to analyze (comma-separated)
# If not provided, the script will prompt for usernames or read from github_usernames.txt
GITHUB_USERNAMES=username1,username2,username3

# Optional: Analysis time period in months (empty for all-time)
TIME_PERIOD=5

# Optional: Output filename
OUTPUT_FILE=github_contributions.xlsx

# Debug settings (true/false)
DEBUG=false
