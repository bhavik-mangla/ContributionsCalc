# GitHub Personal Access Token (Requires scopes: repo, read:org, read:user)
GITHUB_TOKEN=

# Comma-separated list of organizations to analyze
GITHUB_ORGS=AOSSIE-Org

# Optional: Comma-separated list of usernames to analyze
# If left empty, you will need to pass them programmatically
GITHUB_USERS=bmangla